spring.application.name=market-analysis
server.port=8080

# URL of the Python FastAPI ML model service
# Override with ML_SERVICE_URL env var in Docker/production
ml.service.url=${ML_SERVICE_URL:http://localhost:8000}
